---
- name: Deploy application and monitoring
  hosts: webservers
  become: true
  vars_files:
    - ../group_vars/all.yml

  roles:
    - role: web_app
      tags:
        - web_app
    - role: monitoring
      tags:
        - monitoring
