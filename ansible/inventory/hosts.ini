[webservers]
lab04-vm

[webservers:vars]
# Fallback example only.
# The default inventory is inventory/gcp_compute.yml, which discovers the host
# directly from GCP so public IP changes do not require a manual edit here.
ansible_user=ubuntu
ansible_python_interpreter=/usr/bin/python3
